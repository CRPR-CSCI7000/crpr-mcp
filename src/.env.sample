# Zoekt MCP local environment values for docker compose.
# Keep secrets (for example GITHUB_TOKEN) only in this local file.

GITHUB_TOKEN=

# Optional runtime overrides (defaults are defined in src/config.py).
# MCP_SSE_PORT=8000
# MCP_STREAMABLE_HTTP_PORT=8080
# EXECUTION_TIMEOUT_DEFAULT=30
# EXECUTION_TIMEOUT_MAX=120
# EXECUTION_STDOUT_MAX_BYTES=32768
# EXECUTION_STDERR_MAX_BYTES=32768
