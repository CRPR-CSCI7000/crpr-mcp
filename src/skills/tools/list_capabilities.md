---
id: list_capabilities
doc_type: tool
---

List available capabilities or runtime helper ids.
Use this first. The default view returns compact hits across workflows and execution patterns. Use `view="runtime_helpers"` to list runtime helper ids only.
Parameters: - view (optional): `capabilities` (default) or `runtime_helpers`
Output format: - Markdown text (human-readable summary cards).
